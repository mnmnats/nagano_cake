<h2 class="head-line title_h2 text-center">商品詳細</h2>
<div class="row justify-content-center">
  <div class="row">
	  <div class="col-xs-4">
	    <%= image_tag @item.get_image, size: "100x100" %>
    </div>
    <div class="col-xs-6">
     <table class="table">
      <tbody>
        <tr>
          <td>商品名</td>
          <td><%= @item.name %></td>
        </tr>
        <tr>
          <td>商品説明</td>
          <td><%= @item. introduction%></td>
        </tr>
        <tr>
          <td>ジャンル</td>
          <td><%= @item.genre.name %></td>
        </tr>
        <tr>
          <td>税込価格<br>(税抜価格)</td>
          <td><%= (@item.price*1.1).floor.to_s(:delimited, delimiter: ',') %>(<%= (@item.price).floor.to_s(:delimited, delimiter: ',') %>)円</td>
        </tr>
        <tr>
          <td>販売ステータス</td>
          <td><% if !@item.is_active %>
             販売中
           <% else %>
           　販売停止中
           <% end %>
          </td>
        </tr>
       </tbody>
      </table>
      <%= link_to "編集する", edit_admin_item_path(@item.id), class: "glyphicon glyphicon-pencil btn btn-success pull-right" %>
    </div>
  </div>
</div>
